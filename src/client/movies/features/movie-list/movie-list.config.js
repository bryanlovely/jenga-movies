(function() {
    'use strict';

    angular
        .module('movies.movieList')
        .config(configuration);

    function configuration() {
        // TODO: Implementation of configuration in module movies.movieList
    }
})();
