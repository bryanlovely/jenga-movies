'use strict';

// Declare app level module which depends on filters, and services
angular.module('movies.firebase', [
    'firebase.simpleLogin',
    'firebase.directives',
    'firebase.decorators'
  ]);
